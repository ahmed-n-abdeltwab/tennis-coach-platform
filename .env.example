# Database connection URL for PostgreSQL.
# Format: postgresql://<user>:<password>@<host>:<port>/<database>?<options>
# Replace placeholders with your actual credentials and database details.
# The connection URL can include options such as connection pool size and timeout.
DATABASE_URL="postgresql://postgres:password@localhost:5432/tennis_coach_dev?connection_limit=5&pool_timeout=2"

# connection_limit defines the maximum number of active connections
# pooled by Prisma to your database.
# Recommended formula: num_physical_cpus * 2 + 1 (adjust as needed).
# In this example, it is set to 5.
# This setting controls resource usage and concurrency.
# Example: connection_limit=5

# pool_timeout (in seconds) defines how long Prisma will wait
# to acquire a connection from the pool before throwing an error.
# Default is 10 seconds. Lowering this reduces wait but may
# increase errors under heavy load.
# In this example, set to 2 seconds.
# Example: pool_timeout=2

# JWT Configuration
# JWT_SECRET: Secret key for signing access tokens (minimum 32 characters)
# Security best practice: Use a cryptographically secure random string
# Generate with: openssl rand -base64 32
JWT_SECRET="your-super-secure-jwt-secret-key-change-in-production"
JWT_EXPIRES_IN="24h"

# JWT Refresh Token Configuration
# JWT_REFRESH_SECRET: Separate secret key for refresh tokens (minimum 32 characters)
# MUST be different from JWT_SECRET for security
# Generate with: openssl rand -base64 32
JWT_REFRESH_SECRET="your-super-secure-jwt-refresh-secret-key-change-in-production"
JWT_REFRESH_EXPIRES_IN="7d"

# PayPal
PAYPAL_CLIENT_ID="your-paypal-client-id"
PAYPAL_CLIENT_SECRET="your-paypal-client-secret"
PAYPAL_ENVIRONMENT="sandbox"

# Google Calendar
GOOGLE_CLIENT_ID="your-google-oauth-client-id"
GOOGLE_CLIENT_SECRET="your-google-oauth-client-secret"
GOOGLE_REDIRECT_URI="http://localhost:3333/auth/google/callback"

# Email (Nodemailer)
SMTP_SENDER_EMAIL="<SENDER@YOURDOMAIN.COM>"
SMTP_TOKEN="<YOUR-TOKEN-HERE>"

# App Config
PORT=3333
FRONTEND_URL="http://localhost:4200"
NODE_ENV="development"
