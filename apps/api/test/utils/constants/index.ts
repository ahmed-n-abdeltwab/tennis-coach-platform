/**
 * Test Constants
 *
 * Provides default values and constants used across test utilities.
 * These constants ensure consistency in test data creation.
 *
 * Includes:
 * - DEFAULT_TEST_USER: Default user properties
 * - DEFAULT_TEST_COACH: Default coach properties
 * - DEFAULT_TEST_SESSION: Default session properties
 * - DEFAULT_TEST_BOOKING_TYPE: Default booking type properties
 * - DEFAULT_TEST_TIME_SLOT: Default time slot properties
 * - DEFAULT_TEST_DISCOUNT: Default discount properties
 * - DEFAULT_TEST_MESSAGE: Default message properties
 *
 * @example
 * ```typescript
 * import { DEFAULT_TEST_USER, DEFAULT_TEST_COACH } from '@test-utils/constants';
 *
 * const userData = {
 *   ...DEFAULT_TEST_USER,
 *   email: 'custom@example.com',
 * };
 * ```
 *
 * @module constants
 */

export * from './test-constants';
