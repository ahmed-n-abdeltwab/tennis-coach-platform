/**
 * Authentication type definitions for testing
 */

/**
 * Authentication headers structure
 * Used for adding Bearer token authentication to HTTP requests
 */
export interface AuthHeaders {
  Authorization: string;
}
