/**
 * @deprecated: use UserRole and AdminRole from @prisma/client instead
 */
export enum UserType {
  USER = 'USER',
  COACH = 'COACH',
}

/**
 * @deprecated: use UserRole and AdminRole from @prisma/client instead
 */
export enum Role {
  USER = 'USER',
  PREMIUM_USER = 'PREMIUM_USER',
  ADMIN = 'ADMIN',
  COACH = 'COACH',
}
