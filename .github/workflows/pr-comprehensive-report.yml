name: PR - Comprehensive Report

on:
  workflow_run:
    workflows: ["CI"]
    types: [completed]

permissions:
  contents: read
  pull-requests: write
  actions: read
  checks: read

jobs:
  comprehensive-report:
    name: Generate Comprehensive PR Report
    runs-on: ubuntu-latest
    if: github.event.workflow_run.event == 'pull_request'

    steps:
      - name: Get PR number
        id: pr
        run: |
          PR_NUMBER=$(gh api repos/${{ github.repository }}/actions/runs/${{ github.event.workflow_run.id }} --jq '.pull_requests[0].number')
          echo "number=$PR_NUMBER" >> $GITHUB_OUTPUT

          if [ -z "$PR_NUMBER" ] || [ "$PR_NUMBER" == "null" ]; then
            echo "No PR found for this workflow run"
            exit 0
          fi
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Checkout PR code
        uses: actions/checkout@v6
        with:
          ref: refs/pull/${{ steps.pr.outputs.number }}/head
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: "10"
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "20"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Set up Nx SHAs for affected detection
        uses: nrwl/nx-set-shas@v4

      - name: Get affected projects
        id: affected
        run: |
          set -e

          AFFECTED_APPS=$(pnpm exec nx show projects --affected --type app 2>/dev/null || true)
          AFFECTED_LIBS=$(pnpm exec nx show projects --affected --type lib 2>/dev/null || true)

          APP_COUNT=0
          LIB_COUNT=0

          if [ -n "$AFFECTED_APPS" ]; then
            APP_COUNT=$(echo "$AFFECTED_APPS" | grep -c . || echo 0)
          fi

          if [ -n "$AFFECTED_LIBS" ]; then
            LIB_COUNT=$(echo "$AFFECTED_LIBS" | grep -c . || echo 0)
          fi

          TOTAL_COUNT=$((APP_COUNT + LIB_COUNT))

          echo "total=$TOTAL_COUNT" >> $GITHUB_OUTPUT
          echo "apps=$APP_COUNT" >> $GITHUB_OUTPUT
          echo "libs=$LIB_COUNT" >> $GITHUB_OUTPUT

          # Save for later
          echo "$AFFECTED_APPS" > affected_apps.txt
          echo "$AFFECTED_LIBS" > affected_libs.txt

      - name: Download test artifacts
        uses: actions/download-artifact@v7
        with:
          pattern: test-reports-*
          merge-multiple: true
          path: test-reports
          run-id: ${{ github.event.workflow_run.id }}
          github-token: ${{ github.token }}
        continue-on-error: true

      - name: Get test results from CI
        id: tests
        run: |
          # Get workflow conclusion
          if [ "${{ github.event.workflow_run.conclusion }}" == "success" ]; then
            echo "all_passed=true" >> $GITHUB_OUTPUT
          else
            echo "all_passed=false" >> $GITHUB_OUTPUT
          fi

      - name: Generate comprehensive report
        id: report
        run: |
          set -e

          # Start report
          echo "# 🚀 PR Analysis Report" > report.md
          echo "" >> report.md

          # Overall status
          if [ "${{ steps.tests.outputs.all_passed }}" == "true" ]; then
            echo "## ✅ All Checks Passed!" >> report.md
          else
            echo "## ⚠️ Some Checks Need Attention" >> report.md
          fi
          echo "" >> report.md

          # Affected projects section
          echo "## 📦 Affected Projects" >> report.md
          echo "" >> report.md

          TOTAL=${{ steps.affected.outputs.total }}
          if [ "$TOTAL" -eq 0 ]; then
            echo "✅ No projects affected - changes are in non-project files" >> report.md
          else
            echo "🎯 **${TOTAL} project(s) affected**" >> report.md
            echo "" >> report.md

            if [ ${{ steps.affected.outputs.apps }} -gt 0 ]; then
              echo "**Applications (${{ steps.affected.outputs.apps }}):**" >> report.md
              echo '```' >> report.md
              cat affected_apps.txt >> report.md
              echo '```' >> report.md
              echo "" >> report.md
            fi

            if [ ${{ steps.affected.outputs.libs }} -gt 0 ]; then
              echo "**Libraries (${{ steps.affected.outputs.libs }}):**" >> report.md
              echo '```' >> report.md
              cat affected_libs.txt >> report.md
              echo '```' >> report.md
              echo "" >> report.md
            fi
          fi

          # Test results section
          echo "## 🧪 Test Results" >> report.md
          echo "" >> report.md

          if [ "${{ github.event.workflow_run.conclusion }}" == "success" ]; then
            echo "✅ All test suites passed" >> report.md
          elif [ "${{ github.event.workflow_run.conclusion }}" == "failure" ]; then
            echo "❌ Some test suites failed" >> report.md
          else
            echo "⚠️ Test status: ${{ github.event.workflow_run.conclusion }}" >> report.md
          fi
          echo "" >> report.md

          # Check for coverage in artifacts
          if [ -d "test-reports" ]; then
            echo "📊 Coverage reports available in workflow artifacts" >> report.md
          else
            echo "ℹ️ Coverage reports will be available after CI completes" >> report.md
          fi
          echo "" >> report.md

          # Build info
          echo "## 🏗️ Build Information" >> report.md
          echo "" >> report.md
          echo "- **Workflow**: ${{ github.event.workflow_run.name }}" >> report.md
          echo "- **Status**: ${{ github.event.workflow_run.conclusion }}" >> report.md
          echo "- **PR**: #${{ steps.pr.outputs.number }}" >> report.md
          echo "" >> report.md

          # Quick actions
          echo "## 🔗 Quick Actions" >> report.md
          echo "" >> report.md
          echo "- [View CI Workflow Run](${{ github.event.workflow_run.html_url }})" >> report.md
          echo "- [View This Report Run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})" >> report.md
          echo "- [Download Test Reports](${{ github.event.workflow_run.html_url }})" >> report.md
          echo "" >> report.md

          # Tips section
          echo "<details>" >> report.md
          echo "<summary>💡 <b>Tips for local development</b></summary>" >> report.md
          echo "" >> report.md
          echo "- See affected projects: \`pnpm exec nx show projects --affected\`" >> report.md
          echo "- Run affected tests: \`pnpm exec nx affected -t test\`" >> report.md
          echo "- View dependency graph: \`pnpm exec nx graph --affected\`" >> report.md
          echo "- Run with coverage: \`pnpm exec nx run api:test:unit -- --coverage\`" >> report.md
          echo "" >> report.md
          echo "</details>" >> report.md
          echo "" >> report.md

          # Footer
          echo "---" >> report.md
          echo "_🤖 Generated by PR Analysis Bot • [Powered by GitHub Actions](https://github.com/features/actions)_" >> report.md

          # Save to summary
          cat report.md > $GITHUB_STEP_SUMMARY

      - name: Comment PR
        if: steps.pr.outputs.number != ''
        uses: thollander/actions-comment-pull-request@v3
        with:
          filePath: report.md
          comment_tag: comprehensive-pr-report
          mode: recreate
          pr_number: ${{ steps.pr.outputs.number }}
