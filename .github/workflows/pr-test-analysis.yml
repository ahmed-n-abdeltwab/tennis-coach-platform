name: PR Test Analysis & Report

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write
  actions: read
  checks: read

jobs:
  analyze-and-report:
    name: Analyze Tests & Generate Report
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: "10"
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "20"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Set up Nx SHAs for affected detection
        uses: nrwl/nx-set-shas@v4

      - name: Get affected projects
        id: affected
        run: |
          set -e

          AFFECTED_APPS=$(pnpm exec nx show projects --affected --type app 2>/dev/null || true)
          AFFECTED_LIBS=$(pnpm exec nx show projects --affected --type lib 2>/dev/null || true)

          APP_COUNT=0
          LIB_COUNT=0

          [ -n "$AFFECTED_APPS" ] && APP_COUNT=$(echo "$AFFECTED_APPS" | grep -c . || echo 0)
          [ -n "$AFFECTED_LIBS" ] && LIB_COUNT=$(echo "$AFFECTED_LIBS" | grep -c . || echo 0)

          echo "total=$((APP_COUNT + LIB_COUNT))" >> $GITHUB_OUTPUT
          echo "apps=$APP_COUNT" >> $GITHUB_OUTPUT
          echo "libs=$LIB_COUNT" >> $GITHUB_OUTPUT

          echo "$AFFECTED_APPS" > affected_apps.txt
          echo "$AFFECTED_LIBS" > affected_libs.txt

      - name: Wait for CI checks
        id: ci_status
        run: |
          MAX_WAIT=1800
          WAIT_INTERVAL=15
          ELAPSED=0

          while [ $ELAPSED -lt $MAX_WAIT ]; do
            RESPONSE=$(gh api repos/${{ github.repository }}/commits/${{ github.event.pull_request.head.sha }}/check-runs)
            CI_CHECKS=$(echo "$RESPONSE" | jq -r '.check_runs[] | select(.name != "Analyze Tests & Generate Report") | select(.name | test("Test -|Build|Lint|Security")) | "\(.name):\(.status):\(.conclusion)"')

            [ -z "$CI_CHECKS" ] && { sleep $WAIT_INTERVAL; ELAPSED=$((ELAPSED + WAIT_INTERVAL)); continue; }

            PENDING=$(echo "$CI_CHECKS" | grep -c "in_progress\|queued" || true)

            if [ "$PENDING" -eq 0 ]; then
              FAILURES=$(echo "$CI_CHECKS" | grep -c ":failure" || true)
              echo "all_passed=$([ "$FAILURES" -eq 0 ] && echo "true" || echo "false")" >> $GITHUB_OUTPUT
              break
            fi

            sleep $WAIT_INTERVAL
            ELAPSED=$((ELAPSED + WAIT_INTERVAL))
          done

          [ $ELAPSED -ge $MAX_WAIT ] && echo "all_passed=unknown" >> $GITHUB_OUTPUT
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Download test artifacts from CI
        id: download
        run: |
          CI_RUN_ID=$(gh api repos/${{ github.repository }}/actions/workflows/ci.yml/runs \
            --jq ".workflow_runs[] | select(.head_sha == \"${{ github.event.pull_request.head.sha }}\") | .id" | head -1)

          if [ -z "$CI_RUN_ID" ]; then
            echo "⚠️  No CI run found"
            exit 0
          fi

          echo "ci_run_id=$CI_RUN_ID" >> $GITHUB_OUTPUT

          gh api repos/${{ github.repository }}/actions/runs/$CI_RUN_ID/jobs \
            --jq '.jobs[] | select(.name | test("Test -")) | {name: .name, id: .id}' > job_info.json

          for test_type in unit integration e2e; do
            JOB_ID=$(jq -r "select(.name | test(\"$test_type\")) | .id" job_info.json | head -1)
            echo "${test_type}_job_id=$JOB_ID" >> $GITHUB_OUTPUT
            gh run download $CI_RUN_ID --dir ./downloaded-reports --name "test-reports-${test_type}" 2>&1 || true
          done

          if [ ! -d "./downloaded-reports" ]; then
            exit 0
          fi

          mkdir -p test-reports coverage/apps/api/{unit,integration,e2e}

          find ./downloaded-reports -type f -name "*.xml" -exec cp {} test-reports/ \;
          find ./downloaded-reports -type f -name "test-report-*.json" -exec cp {} test-reports/ \;

          for test_type in unit integration e2e; do
            COVERAGE_FILE=$(find ./downloaded-reports -type f -path "*/api/${test_type}/coverage-summary.json" | head -1)
            [ -n "$COVERAGE_FILE" ] && cp "$COVERAGE_FILE" "coverage/apps/api/${test_type}/coverage-summary.json"
          done
        env:
          GH_TOKEN: ${{ github.token }}
        continue-on-error: true

      - name: Analyze test artifacts
        id: analyze
        run: |
          node scripts/analyze-test-artifacts.js test-reports/analysis.json
          echo "analysis_available=$([ -f "test-reports/analysis.json" ] && echo "true" || echo "false")" >> $GITHUB_OUTPUT

      - name: Generate PR report
        id: report
        run: |
          set -e

          ANALYSIS_FILE="test-reports/analysis.json"
          CI_RUN_ID="${{ steps.download.outputs.ci_run_id }}"
          REPO="${{ github.repository }}"
          PR_NUM="${{ github.event.number }}"

          QUALITY_EXCELLENT=80
          QUALITY_GOOD=60

          create_progress_bar() {
            local pct=$1
            local filled=$(awk "BEGIN {print int($pct / 5)}")
            local empty=$((20 - filled))
            printf "[%s%s]" "$(printf '█%.0s' $(seq 1 $filled))" "$(printf '░%.0s' $(seq 1 $empty))"
          }

          get_quality_badge() {
            local pct=$1
            if [ "$pct" != "N/A" ]; then
              local num=$(echo "$pct" | awk '{print int($1)}')
              if [ "$num" -ge $QUALITY_EXCELLENT ]; then
                echo "🟢 Excellent"
              elif [ "$num" -ge $QUALITY_GOOD ]; then
                echo "🟡 Good"
              else
                echo "🔴 Needs Work"
              fi
            else
              echo "⚪ N/A"
            fi
          }

          get_job_id() {
            local test_type=$1
            case "$test_type" in
              unit) echo "${{ steps.download.outputs.unit_job_id }}" ;;
              integration) echo "${{ steps.download.outputs.integration_job_id }}" ;;
              e2e) echo "${{ steps.download.outputs.e2e_job_id }}" ;;
            esac
          }

          {
            echo "# 🎯 Test & Coverage Report"
            echo ""

            case "${{ steps.ci_status.outputs.all_passed }}" in
              true) echo "## ✅ All Tests Passed" ;;
              unknown) echo "## ⏳ Tests In Progress" ;;
              *) echo "## ❌ Tests Failed" ;;
            esac
            echo ""

            if [ "${{ steps.analyze.outputs.analysis_available }}" != "true" ]; then
              echo "> ⚠️ Test analysis data not available yet"
              echo ""
            else
              TOTAL_TESTS=$(jq -r '.summary.aggregate.total.tests' "$ANALYSIS_FILE")
              TOTAL_PASSED=$(jq -r '.summary.aggregate.total.passed' "$ANALYSIS_FILE")
              TOTAL_FAILED=$(jq -r '.summary.aggregate.total.failed' "$ANALYSIS_FILE")
              TOTAL_SKIPPED=$(jq -r '.summary.aggregate.total.skipped' "$ANALYSIS_FILE")
              PASS_RATE=$(jq -r '.summary.aggregate.total.passRate' "$ANALYSIS_FILE")

              echo "## 📊 Test Suite Overview"
              echo ""
              echo "| Suite | Tests | Pass Rate | Duration | Lines | Statements | Functions | Branches | Quality | Status |"
              echo "|-------|-------|-----------|----------|-------|------------|-----------|----------|---------|--------|"

              for test_type in unit integration e2e; do
                HAS_DATA=$(jq -r ".testTypes.${test_type}.hasData" "$ANALYSIS_FILE")

                if [ "$HAS_DATA" == "true" ]; then
                  TESTS=$(jq -r ".testTypes.${test_type}.junit.tests // 0" "$ANALYSIS_FILE")
                  PASSED=$(jq -r ".testTypes.${test_type}.junit.passed // 0" "$ANALYSIS_FILE")
                  FAILED=$(jq -r ".testTypes.${test_type}.junit.failures // 0" "$ANALYSIS_FILE")
                  DURATION=$(jq -r ".testTypes.${test_type}.junit.time // 0" "$ANALYSIS_FILE")

                  PASS_RATE=$([ "$TESTS" -gt 0 ] && awk "BEGIN {printf \"%.1f\", ($PASSED / $TESTS) * 100}" || echo "N/A")

                  COV_LINES=$(jq -r ".testTypes.${test_type}.coverage.lines.pct // \"N/A\"" "$ANALYSIS_FILE")
                  COV_STATEMENTS=$(jq -r ".testTypes.${test_type}.coverage.statements.pct // \"N/A\"" "$ANALYSIS_FILE")
                  COV_FUNCTIONS=$(jq -r ".testTypes.${test_type}.coverage.functions.pct // \"N/A\"" "$ANALYSIS_FILE")
                  COV_BRANCHES=$(jq -r ".testTypes.${test_type}.coverage.branches.pct // \"N/A\"" "$ANALYSIS_FILE")

                  QUALITY=$(get_quality_badge "$COV_LINES")

                  [ "$TESTS" == "0" ] && TESTS="N/A"
                  [ "$PASS_RATE" != "N/A" ] && PASS_RATE="${PASS_RATE}%"
                  [ "$DURATION" == "0" ] && DURATION="N/A" || DURATION="${DURATION}s"
                  [ "$COV_LINES" != "N/A" ] && COV_LINES="${COV_LINES}%"
                  [ "$COV_STATEMENTS" != "N/A" ] && COV_STATEMENTS="${COV_STATEMENTS}%"
                  [ "$COV_FUNCTIONS" != "N/A" ] && COV_FUNCTIONS="${COV_FUNCTIONS}%"
                  [ "$COV_BRANCHES" != "N/A" ] && COV_BRANCHES="${COV_BRANCHES}%"
                else
                  TESTS="N/A"
                  PASS_RATE="N/A"
                  DURATION="N/A"
                  COV_LINES=""
                  COV_STATEMENTS=""
                  COV_FUNCTIONS=""
                  COV_BRANCHES=""
                  QUALITY="🔴 Needs Work"
                  FAILED=1
                fi

                JOB_ID=$(get_job_id "$test_type")

                if [ "$TESTS" == "N/A" ] || [ -z "$COV_LINES" ]; then
                  STATUS="[⚠️ Partial](https://github.com/${REPO}/actions/runs/${CI_RUN_ID}/job/${JOB_ID})"
                elif [ "$FAILED" -gt 0 ]; then
                  STATUS="[❌ Failed](https://github.com/${REPO}/actions/runs/${CI_RUN_ID}/job/${JOB_ID})"
                else
                  STATUS="[✅ Passed](https://github.com/${REPO}/actions/runs/${CI_RUN_ID}/job/${JOB_ID})"
                fi

                echo "| **${test_type^}** | $TESTS | $PASS_RATE | $DURATION | $COV_LINES | $COV_STATEMENTS | $COV_FUNCTIONS | $COV_BRANCHES | $QUALITY | $STATUS |"
              done

              echo ""
              echo "> **Quality Legend:** 🟢 Excellent (≥${QUALITY_EXCELLENT}%) • 🟡 Good (≥${QUALITY_GOOD}%) • 🔴 Needs Work (<${QUALITY_GOOD}%)"
              echo ""

              echo "## 📈 Summary"
              echo ""
              echo "| Metric | Value |"
              echo "|--------|-------|"
              echo "| **Total Tests** | $TOTAL_TESTS |"
              echo "| ✅ Passed | $TOTAL_PASSED |"
              echo "| ❌ Failed | $TOTAL_FAILED |"
              echo "| ⏭️ Skipped | $TOTAL_SKIPPED |"
              echo "| **Pass Rate** | ${PASS_RATE}% |"
              echo ""

              for test_type in unit integration e2e; do
                HAS_DATA=$(jq -r ".testTypes.${test_type}.hasData" "$ANALYSIS_FILE")
                [ "$HAS_DATA" != "true" ] && continue

                JOB_ID=$(get_job_id "$test_type")

                echo "<details>"
                echo "<summary><b>📋 ${test_type^} Tests</b> — Detailed Results</summary>"
                echo ""

                TESTS=$(jq -r ".testTypes.${test_type}.junit.tests // 0" "$ANALYSIS_FILE")
                PASSED=$(jq -r ".testTypes.${test_type}.junit.passed // 0" "$ANALYSIS_FILE")
                FAILED=$(jq -r ".testTypes.${test_type}.junit.failures // 0" "$ANALYSIS_FILE")
                SKIPPED=$(jq -r ".testTypes.${test_type}.junit.skipped // 0" "$ANALYSIS_FILE")
                DURATION=$(jq -r ".testTypes.${test_type}.junit.time // 0" "$ANALYSIS_FILE")

                if [ "$TESTS" -gt 0 ]; then
                  SUITE_PASS_RATE=$(awk "BEGIN {printf \"%.1f\", ($PASSED / $TESTS) * 100}")

                  echo "### Test Execution"
                  echo ""
                  echo "| Metric | Value |"
                  echo "|--------|-------|"
                  echo "| Total | $TESTS |"
                  echo "| ✅ Passed | $PASSED |"
                  echo "| ❌ Failed | $FAILED |"
                  echo "| ⏭️ Skipped | $SKIPPED |"
                  echo "| Success Rate | ${SUITE_PASS_RATE}% |"
                  echo "| Duration | ${DURATION}s |"
                  echo ""
                else
                  echo "### Test Execution"
                  echo ""
                  echo "⚠️ No test results found"
                  echo ""
                fi

                COV_LINES_PCT=$(jq -r ".testTypes.${test_type}.coverage.lines.pct // null" "$ANALYSIS_FILE")

                if [ "$COV_LINES_PCT" != "null" ]; then
                  COV_LINES_COVERED=$(jq -r ".testTypes.${test_type}.coverage.lines.covered" "$ANALYSIS_FILE")
                  COV_LINES_TOTAL=$(jq -r ".testTypes.${test_type}.coverage.lines.total" "$ANALYSIS_FILE")
                  COV_STATEMENTS_PCT=$(jq -r ".testTypes.${test_type}.coverage.statements.pct" "$ANALYSIS_FILE")
                  COV_FUNCTIONS_PCT=$(jq -r ".testTypes.${test_type}.coverage.functions.pct" "$ANALYSIS_FILE")
                  COV_BRANCHES_PCT=$(jq -r ".testTypes.${test_type}.coverage.branches.pct" "$ANALYSIS_FILE")

                  COV_NUM=$(echo "$COV_LINES_PCT" | awk '{print int($1)}')
                  if [ "$COV_NUM" -ge $QUALITY_EXCELLENT ]; then
                    BADGE="🟢"
                  elif [ "$COV_NUM" -ge $QUALITY_GOOD ]; then
                    BADGE="🟡"
                  else
                    BADGE="🔴"
                  fi

                  echo "### Code Coverage $BADGE"
                  echo ""
                  echo "| Metric | Coverage | Visual |"
                  echo "|--------|----------|--------|"
                  echo "| **Lines** | **${COV_LINES_PCT}%** | $(create_progress_bar $COV_LINES_PCT) |"
                  echo "| Statements | ${COV_STATEMENTS_PCT}% | $(create_progress_bar $COV_STATEMENTS_PCT) |"
                  echo "| Functions | ${COV_FUNCTIONS_PCT}% | $(create_progress_bar $COV_FUNCTIONS_PCT) |"
                  echo "| Branches | ${COV_BRANCHES_PCT}% | $(create_progress_bar $COV_BRANCHES_PCT) |"
                  echo ""
                  echo "_${COV_LINES_COVERED} of ${COV_LINES_TOTAL} lines covered • $((COV_LINES_TOTAL - COV_LINES_COVERED)) lines remaining_"
                  echo ""
                else
                  echo "### Code Coverage ⚠️"
                  echo ""
                  echo "> **No coverage data available**"
                  echo ">"
                  echo "> This usually indicates:"
                  echo "> - Tests crashed before coverage collection"
                  echo "> - Coverage was not enabled for this run"
                  echo "> - Check the [test logs](https://github.com/${REPO}/actions/runs/${CI_RUN_ID}/job/${JOB_ID}) for details"
                  echo ""
                fi

                if [ -n "$JOB_ID" ] && [ -n "$CI_RUN_ID" ]; then
                  echo "---"
                  echo ""
                  echo "**📊 Full Test Output:** [View complete ${test_type} test logs →](https://github.com/${REPO}/actions/runs/${CI_RUN_ID}/job/${JOB_ID})"
                  echo ""
                fi

                echo "</details>"
                echo ""
              done
            fi

            TOTAL=${{ steps.affected.outputs.total }}
            if [ "$TOTAL" -gt 0 ]; then
              echo "---"
              echo ""
              echo "## 📦 Impact Analysis"
              echo ""
              echo "<details>"
              echo "<summary><b>${TOTAL} projects affected</b> by this PR</summary>"
              echo ""

              if [ ${{ steps.affected.outputs.apps }} -gt 0 ]; then
                echo "**Applications** (${{ steps.affected.outputs.apps }})"
                echo '```'
                cat affected_apps.txt
                echo '```'
                echo ""
              fi

              if [ ${{ steps.affected.outputs.libs }} -gt 0 ]; then
                echo "**Libraries** (${{ steps.affected.outputs.libs }})"
                echo '```'
                cat affected_libs.txt
                echo '```'
              fi

              echo "</details>"
              echo ""
            fi

            echo "---"
            echo ""
            echo "## 📋 Metadata"
            echo ""
            echo "| | |"
            echo "|---|---|"
            echo "| **PR** | #${PR_NUM} |"
            echo "| **Branch** | \`${{ github.head_ref }}\` |"
            echo "| **Commit** | \`${{ github.event.pull_request.head.sha }}\` |"
            [ -n "$CI_RUN_ID" ] && echo "| **CI Run** | [View →](https://github.com/${REPO}/actions/runs/${CI_RUN_ID}) |"
            echo ""

            echo "<details>"
            echo "<summary><b>🔗 Quick Links</b></summary>"
            echo ""
            echo "- [All Checks](https://github.com/${REPO}/pull/${PR_NUM}/checks)"
            echo "- [Files Changed](https://github.com/${REPO}/pull/${PR_NUM}/files)"
            echo "- [Conversation](https://github.com/${REPO}/pull/${PR_NUM})"
            echo ""
            echo "</details>"
            echo ""

            echo "<details>"
            echo "<summary><b>💡 Developer Commands</b></summary>"
            echo ""
            echo "\`\`\`bash"
            echo "# See what's affected"
            echo "pnpm exec nx show projects --affected"
            echo ""
            echo "# Run affected tests"
            echo "pnpm exec nx affected -t test"
            echo ""
            echo "# View dependency graph"
            echo "pnpm exec nx graph --affected"
            echo ""
            echo "# Run with coverage"
            echo "pnpm exec nx run api:test:unit -- --coverage"
            echo "\`\`\`"
            echo ""
            echo "</details>"
            echo ""

            echo "---"
            echo ""
            echo "<sub>🤖 Automated by [Test Analysis Bot](https://github.com/${REPO}/blob/${{ github.head_ref }}/.github/workflows/pr-test-analysis.yml) • Report generated in ${{ github.run_id }}</sub>"
          } > report.md

          cat report.md > $GITHUB_STEP_SUMMARY

      - name: Upload analysis artifact
        if: steps.analyze.outputs.analysis_available == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: test-analysis
          path: test-reports/analysis.json
          retention-days: 30

      - name: Comment on PR
        uses: thollander/actions-comment-pull-request@v3
        with:
          file-path: report.md
          comment-tag: comprehensive-pr-report
          mode: recreate
